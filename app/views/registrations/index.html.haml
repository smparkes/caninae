%h1 Listing registrations

%table
  %tr
    %th Registry
    %th Animal
    %th Registration number
    %th Date
    %th
    %th
    %th

  - @registrations.each do |registration|
    %tr
      %td= registration.registry_id
      %td= registration.animal_id
      %td= registration.registration_number
      %td= registration.date
      %td= link_to 'Show', registration
      %td= link_to 'Edit', edit_registration_path(registration)
      %td= link_to 'Destroy', registration, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Registration', new_registration_path
